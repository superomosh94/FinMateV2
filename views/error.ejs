<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Error - FinMate' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card shadow">
                    <div class="card-body text-center">
                        <h1 class="text-danger">⚠️</h1>
                        <h2 class="mb-4">Something went wrong</h2>
                        <p class="lead mb-4"><%= message || 'An unexpected error occurred' %></p>
                        
                        <!-- FIXED: Check if error exists before accessing its properties -->
                        <% if (typeof error !== 'undefined' && error && error.stack) { %>
                            <% if (typeof process !== 'undefined' && process.env && process.env.NODE_ENV === 'development') { %>
                                <div class="alert alert-danger text-start">
                                    <pre><%= error.stack %></pre>
                                </div>
                            <% } else { %>
                                <div class="alert alert-warning">
                                    <p>Error details are hidden in production mode.</p>
                                </div>
                            <% } %>
                        <% } %>
                        
                        <div class="mt-4">
                            <a href="/" class="btn btn-primary me-2">Go Home</a>
                            <a href="/auth/login" class="btn btn-outline-secondary">Login</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>